<?php
    $redirect = "";

    // $_POST validation
    // $_POST sanitization
    // Ensure correct value types
    // Quantities can't be below 1
    // Set $_SESSION vars for errors and sanitized post data
    // If everything is sanitized, send to success screen with posted values, else pass back to index

    var_dump($_POST);
?>